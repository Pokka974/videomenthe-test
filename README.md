# Installation

in /back folder

```
npm i
npm start
```

in /front folder

```
npm i
npm start
```

## Add a `.env` file in the front/ folder with a `REACT_APP_API_URL` value ( http://localhost:PORT in local )
